{
    "version": 1,
    "name": "quasar-edtech",
    "builds": [
       { "backend": "server.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "backend": "/(.*)", "dest": "/server.js" }
    ]
 }